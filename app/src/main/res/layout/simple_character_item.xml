<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    app:cardCornerRadius="12dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/imageContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:cardCornerRadius="12dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/image"
            android:layout_width="@dimen/resident_image_width_height"
            android:layout_height="@dimen/resident_image_width_height" />
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:background="@drawable/base_circle_background"
        android:backgroundTint="@color/white_translucent"
        android:ellipsize="end"
        android:fontFamily="@font/alatsi"
        android:maxLines="2"
        android:paddingHorizontal="8dp"
        android:paddingTop="2dp"
        android:paddingBottom="3dp"
        android:textAlignment="center"
        android:textColor="@color/rick_and_morty_purple_dark"
        android:textSize="11sp"
        app:layout_constraintEnd_toEndOf="@id/imageContainer"
        app:layout_constraintStart_toStartOf="@id/imageContainer"
        app:layout_constraintTop_toBottomOf="@id/imageContainer"
        tools:text="Nice Rick" />
</androidx.constraintlayout.widget.ConstraintLayout>